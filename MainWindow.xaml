<Window x:Class="Car_sharingDB.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Каршеринг — Управление" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Боковая панель навигации -->
        <Border Grid.Column="0" Background="#2C3E50">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="КАРШЕРИНГ" Foreground="White" FontSize="16"
                           FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <Button x:Name="BtnVehicles" Content="🚗  Автомобили"
                        Style="{StaticResource NavButton}" Click="BtnVehicles_Click"/>
                <Button x:Name="BtnClients" Content="👤  Клиенты"
                        Style="{StaticResource NavButton}" Click="BtnClients_Click"/>
                <Button x:Name="BtnRent" Content="📋  Аренда"
                        Style="{StaticResource NavButton}" Click="BtnRent_Click"/>
                <Button x:Name="BtnPayments" Content="💳  Платежи"
                        Style="{StaticResource NavButton}" Click="BtnPayments_Click"/>
            </StackPanel>
        </Border>

        <!-- Область контента -->
        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>

</Window>
